
  .root {
    --bg: #ffffff;
    --bg-2: #f3f5fa;
    --ink: #2b2f36;
    --ink-2: #6b7280;
    --rule: #e5e7ef;
    --blue: #3aa0e8;
    --blue-deep: #1f7cc2;
    --blue-soft: #c7e3f7;
    --amber: #e8b54b;
    --amber-soft: #f3d893;
    --accent: #3aa0e8;
  }

  .root * { box-sizing: border-box; }
  .root {
    position: relative;
    min-height: 100%;
    background:
      radial-gradient(1200px 600px at 50% -10%, #ffffff 0%, #f7f9fc 60%, var(--bg-2) 100%);
    color: var(--ink);
    font-family: "Inter Tight", ui-sans-serif, system-ui, -apple-system, Helvetica, Arial, sans-serif;
    font-feature-settings: "ss01","cv11";
    -webkit-font-smoothing: antialiased;
    display: flex;
    flex-direction: column;
    overflow-x: hidden;
  }

  /* grid paper */
  .root::before {
    content: "";
    position: absolute; inset: 0;
    background-image:
      linear-gradient(to right, rgba(23,32,43,0.04) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(23,32,43,0.04) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: radial-gradient(1000px 700px at 50% 40%, #000 40%, transparent 85%);
    pointer-events: none;
    z-index: 0;
  }

  .topbar {
    position: relative;
    z-index: 3;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 22px 36px;
    border-bottom: 1px solid var(--rule);
    background: rgba(244,241,236,0.6);
    backdrop-filter: blur(6px);
  }
  .brand {
    display: flex; align-items: center; gap: 10px;
    color: var(--ink);
  }
  .brand .mark {
    width: 26px; height: 32px;
    display: block;
    flex: none;
  }
  .brand .wordmark {
    display: flex; flex-direction: column;
    line-height: 1;
    font-family: "Inter Tight", sans-serif;
  }
  .brand .wordmark .top {
    font-weight: 700;
    font-size: 15px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink);
    white-space: nowrap;
  }
  .brand .wordmark .bot {
    margin-top: 4px;
    font-weight: 600;
    font-size: 10px;
    letter-spacing: 0.26em;
    text-transform: uppercase;
    color: var(--blue);
  }
  .meta {
    font-family: "JetBrains Mono", ui-monospace, monospace;
    font-size: 11px;
    color: var(--ink-2);
    display: flex; gap: 18px; align-items: center;
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }
  .dot {
    width: 7px; height: 7px; border-radius: 50%;
    background: var(--amber);
    box-shadow: 0 0 0 3px rgba(232,181,75,0.25);
    animation: pulse 1.8s infinite ease-in-out;
    display: inline-block;
    margin-right: 8px;
    vertical-align: middle;
  }
  @keyframes pulse {
    0%,100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.55; transform: scale(0.85); }
  }
  .main {
    position: relative;
    z-index: 2;
    flex: 1 1 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 40px 24px 0;
  }

  .eyebrow {
    font-family: "JetBrains Mono", ui-monospace, monospace;
    font-size: 12px;
    letter-spacing: 0.22em;
    color: var(--ink-2);
    text-transform: uppercase;
    display: inline-flex; align-items: center;
    padding: 8px 14px;
    border: 1px solid var(--rule);
    border-radius: 999px;
    background: rgba(255,255,255,0.55);
  }

  .h1 {
    font-family: "Inter Tight", sans-serif;
    font-weight: 600;
    font-size: clamp(52px, 8.2vw, 120px);
    line-height: 1.0;
    letter-spacing: -0.04em;
    margin: 10px 0 0;
    color: var(--ink);
    white-space: nowrap;
  }
  h1 .amp {
    color: var(--blue);
    font-style: italic;
    font-weight: 500;
  }

  .subtitle {
    margin-top: 16px;
    font-size: clamp(17px, 1.7vw, 22px);
    color: var(--ink-2);
    max-width: 680px;
    line-height: 1.5;
    text-wrap: balance;
  }
  .subtitle strong { color: var(--ink); font-weight: 600; }

  .ctaRow {
    margin-top: 14px;
    display: flex; gap: 10px; flex-wrap: wrap;
    justify-content: center;
  }
  .btn {
    font: inherit;
    font-size: 14px;
    font-weight: 500;
    padding: 11px 18px;
    border-radius: 10px;
    border: 1px solid var(--rule);
    background: #fff;
    color: var(--ink);
    cursor: pointer;
    transition: transform .15s ease, box-shadow .15s ease, background .15s ease;
    display: inline-flex; align-items: center; gap: 8px;
  }
  .btn:hover { transform: translateY(-1px); box-shadow: 0 6px 20px -10px rgba(23,32,43,0.3); }
  .btn.primary {
    background: var(--ink); color: #fff; border-color: var(--ink);
  }
  .btn.primary:hover { background: #000; }

  /* ====== CONSTRUCTION ZONE ====== */
  .zone {
    position: relative;
    height: 200px;
    width: 100%;
    margin-top: auto;
    flex: 0 0 200px;
    z-index: 2;
  }
  .zone .barricade {
    position: absolute;
    inset: auto 0 0 0;
    height: 14px;
    background:
      repeating-linear-gradient(
        -45deg,
        var(--ink) 0 14px,
        var(--amber) 14px 28px
      );
    opacity: 0.9;
    box-shadow: 0 -2px 0 rgba(0,0,0,0.06);
  }
  .ground {
    display: none;
  }

  .scene {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  /* Scaffolding on the right */
  .scaffold {
    position: absolute;
    right: 6%;
    bottom: 44px;
    width: 160px;
    height: 140px;
    pointer-events: none;
  }
  .scaffold .post {
    position: absolute;
    width: 4px;
    background: var(--ink);
    opacity: 0.75;
    border-radius: 2px;
  }
  .scaffold .post.l { left: 0; top: 0; bottom: 0; }
  .scaffold .post.r { right: 0; top: 0; bottom: 0; }
  .scaffold .rung {
    position: absolute;
    left: 0; right: 0;
    height: 4px;
    background: var(--ink);
    opacity: 0.75;
    border-radius: 2px;
  }

  /* Wall being built */
  .wall {
    position: absolute;
    right: 8%;
    bottom: 44px;
    width: 130px;
    height: 0;
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: 3px;
    align-content: end;
    animation: buildWall 16s ease-in-out infinite;
  }
  @keyframes buildWall {
    0%   { height: 10px; }
    25%  { height: 36px; }
    55%  { height: 72px; }
    78%  { height: 104px; }
    92%  { height: 104px; }
    100% { height: 10px; }
  }
  .brick {
    height: 14px;
    background: linear-gradient(to bottom, #d28a5a, #b86f41);
    border-radius: 2px;
    box-shadow: inset 0 -2px 0 rgba(0,0,0,0.12);
  }

  /* Crane */
  .crane {
    position: absolute;
    left: 5%;
    bottom: 44px;
    width: 180px;
    height: 180px;
    pointer-events: none;
    transform: scale(0.82);
    transform-origin: bottom left;
  }
  .crane .mast {
    position: absolute;
    left: 18px; bottom: 0;
    width: 8px; height: 180px;
    background: var(--ink);
    border-radius: 2px;
  }
  .crane .mast::before, .crane .mast::after {
    content: "";
    position: absolute;
    left: -10px; right: -10px;
    height: 2px;
    background: var(--ink);
  }
  .crane .mast::before { top: 40px; }
  .crane .mast::after  { top: 110px; }

  .crane .arm {
    position: absolute;
    left: 22px;
    top: 20px;
    width: 170px;
    height: 6px;
    background: var(--ink);
    transform-origin: left center;
    animation: swing 6s ease-in-out infinite;
  }
  .crane .arm::before {
    content: "";
    position: absolute;
    left: -14px; top: -8px;
    width: 24px; height: 22px;
    background: var(--amber);
    border-radius: 2px;
  }
  .crane .cable {
    position: absolute;
    top: 6px; right: 24px;
    width: 2px; height: 60px;
    background: var(--ink);
    animation: cable 6s ease-in-out infinite;
  }
  .crane .hook {
    position: absolute;
    right: 14px; top: 60px;
    width: 24px; height: 14px;
    background: var(--blue);
    border-radius: 3px;
    box-shadow: inset 0 -3px 0 var(--blue-deep);
    animation: hookLoad 6s ease-in-out infinite;
  }
  @keyframes swing {
    0%,100% { transform: rotate(-6deg); }
    50% { transform: rotate(6deg); }
  }
  @keyframes cable {
    0%,100% { height: 40px; }
    40%,60% { height: 90px; }
  }
  @keyframes hookLoad {
    0%,100% { top: 40px; transform: scaleY(1); }
    40%,60% { top: 90px; transform: scaleY(1); }
  }

  /* Bots */
  .bot {
    position: absolute;
    bottom: 42px;
    width: 58px;
    height: 62px;
  }
  .bot .head {
    position: absolute;
    left: 8px; top: 0;
    width: 42px; height: 32px;
    background: var(--blue);
    border-radius: 10px 10px 8px 8px;
    box-shadow:
      inset 0 -6px 0 var(--blue-deep),
      inset 3px 3px 0 rgba(255,255,255,0.15);
  }
  .bot .visor {
    position: absolute;
    left: 14px; top: 8px;
    width: 30px; height: 12px;
    background: #0a1b3d;
    border-radius: 6px;
    overflow: hidden;
  }
  .bot .visor::after {
    content: "";
    position: absolute;
    width: 6px; height: 6px;
    background: #8ab4ff;
    border-radius: 50%;
    top: 3px; left: 6px;
    box-shadow: 14px 0 0 #8ab4ff;
    animation: blink 3.5s infinite;
  }
  @keyframes blink {
    0%,92%,100% { opacity: 1; }
    94%,98% { opacity: 0.1; }
  }
  .bot .antenna {
    position: absolute;
    left: 27px; top: -8px;
    width: 4px; height: 10px;
    background: var(--ink);
    border-radius: 2px;
  }
  .bot .antenna::after {
    content: "";
    position: absolute;
    top: -5px; left: -2px;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--amber);
    box-shadow: 0 0 8px var(--amber);
    animation: blinkLight 1.2s infinite;
  }
  @keyframes blinkLight {
    0%,60% { opacity: 1; }
    61%,100% { opacity: 0.25; }
  }
  .bot .body {
    position: absolute;
    left: 12px; top: 28px;
    width: 34px; height: 20px;
    background: var(--blue-deep);
    border-radius: 4px;
    box-shadow: inset 2px 2px 0 rgba(255,255,255,0.08);
  }
  .bot .body::after {
    content: "";
    position: absolute;
    left: 12px; top: 6px;
    width: 10px; height: 8px;
    background: var(--amber);
    border-radius: 1px;
  }
  .bot .treads {
    position: absolute;
    left: 4px; bottom: 0;
    width: 50px; height: 16px;
    background: #0e1a2d;
    border-radius: 4px;
  }
  .bot .treads::before, .bot .treads::after {
    content: "";
    position: absolute;
    top: 3px;
    width: 10px; height: 10px;
    border-radius: 50%;
    background: #2a3a55;
    border: 2px solid #0e1a2d;
  }
  .bot .treads::before { left: 3px; }
  .bot .treads::after  { right: 3px; }
  .bot .arm {
    position: absolute;
    left: 4px; top: 32px;
    width: 14px; height: 5px;
    background: var(--blue-deep);
    border-radius: 2px;
    transform-origin: right center;
  }
  .bot .arm.right {
    left: auto; right: 4px;
    transform-origin: left center;
  }

  /* Bot 1: walking right, carrying plank */
  .bot1 {
    animation: walkRight 14s linear infinite;
  }
  .bot1 .plank {
    position: absolute;
    left: -24px; top: 18px;
    width: 100px; height: 8px;
    background: linear-gradient(to bottom, #c89266, #a8714a);
    border-radius: 2px;
    box-shadow: inset 0 -2px 0 rgba(0,0,0,0.15);
    animation: bobPlank 0.6s ease-in-out infinite;
  }
  .bot1 .arm { transform: rotate(-30deg); }
  .bot1 .arm.right { transform: rotate(30deg); }
  .bot1 .treads { animation: tread 0.3s steps(2) infinite; }

  /* Bot 2: walking left, carrying bricks */
  .bot2 {
    animation: walkLeft 17s linear infinite;
    animation-delay: -4s;
  }
  .bot2 .stack {
    position: absolute;
    left: 14px; top: 10px;
    width: 30px; height: 20px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2px;
    animation: bobPlank 0.5s ease-in-out infinite;
  }
  .bot2 .stack b {
    display: block;
    background: linear-gradient(to bottom, #d28a5a, #b86f41);
    border-radius: 1px;
  }
  .bot2 .arm { transform: rotate(-45deg); }
  .bot2 .arm.right { transform: rotate(45deg); }
  .bot2 .treads { animation: tread 0.3s steps(2) infinite; }

  /* Bot 3: small roller bot pushing beam */
  .bot3 {
    animation: walkRight 19s linear infinite;
    animation-delay: -8s;
  }
  .bot3 .beam {
    position: absolute;
    left: 52px; top: 36px;
    width: 44px; height: 10px;
    background: #7a8596;
    border-radius: 2px;
    box-shadow: inset 0 -2px 0 rgba(0,0,0,0.2);
  }
  .bot3 .arm { transform: rotate(0); }
  .bot3 .arm.right { transform: rotate(0); width: 20px; }
  .bot3 .treads { animation: tread 0.25s steps(2) infinite; }

  /* Bot 4: welding bot — stays near scaffold */
  .bot4 {
    right: 12%;
    left: auto;
    animation: welderBob 2.4s ease-in-out infinite;
  }
  .bot4 .torch {
    position: absolute;
    right: -6px; top: 30px;
    width: 16px; height: 4px;
    background: var(--ink);
    border-radius: 2px;
  }
  .bot4 .torch::after {
    content: "";
    position: absolute;
    right: -8px; top: -3px;
    width: 10px; height: 10px;
    border-radius: 50%;
    background: radial-gradient(circle, #fff6c8 0%, var(--amber) 40%, transparent 70%);
    animation: spark 0.18s steps(2) infinite;
  }
  .bot4 .arm.right { transform: rotate(-10deg); }

  .sparks {
    position: absolute;
    right: 10.5%;
    bottom: 72px;
    width: 40px; height: 40px;
    pointer-events: none;
  }
  .sparks i {
    position: absolute;
    width: 3px; height: 3px;
    background: var(--amber);
    border-radius: 50%;
    opacity: 0;
  }
  .sparks i:nth-child(1) { animation: spark1 1.2s linear infinite; }
  .sparks i:nth-child(2) { animation: spark1 1.2s linear infinite 0.3s; left: 4px; }
  .sparks i:nth-child(3) { animation: spark1 1.2s linear infinite 0.6s; left: -4px; }
  .sparks i:nth-child(4) { animation: spark1 1.2s linear infinite 0.9s; left: 8px; }

  @keyframes spark {
    0% { opacity: 1; transform: scale(1); }
    100% { opacity: 0.4; transform: scale(1.3); }
  }
  @keyframes spark1 {
    0% { opacity: 1; transform: translate(0,0) scale(1); }
    100% { opacity: 0; transform: translate(var(--tx,12px), 24px) scale(0.2); }
  }

  @keyframes walkRight {
    0%   { left: -80px; }
    100% { left: calc(100% + 80px); }
  }
  @keyframes walkLeft {
    0%   { left: calc(100% + 80px); }
    100% { left: -80px; }
  }
  @keyframes welderBob {
    0%,100% { transform: translateY(0); }
    50% { transform: translateY(-2px); }
  }
  @keyframes bobPlank {
    0%,100% { transform: translateY(0) rotate(0); }
    50% { transform: translateY(-1.5px) rotate(0.5deg); }
  }
  @keyframes tread {
    0% { background-position: 0 0; }
    100% { background-position: 8px 0; }
  }

  /* cone */
  .cone {
    position: absolute;
    bottom: 42px;
    width: 22px;
    height: 28px;
  }
  .cone::before {
    content: "";
    position: absolute;
    left: 50%; bottom: 4px;
    transform: translateX(-50%);
    width: 0; height: 0;
    border-left: 10px solid transparent;
    border-right: 10px solid transparent;
    border-bottom: 22px solid var(--amber);
  }
  .cone::after {
    content: "";
    position: absolute;
    left: 2px; bottom: 0;
    width: 18px; height: 4px;
    background: var(--ink);
    border-radius: 1px;
  }
  .cone .band {
    position: absolute;
    left: 50%; bottom: 14px;
    transform: translateX(-50%);
    width: 14px; height: 2px;
    background: #fff;
    z-index: 2;
  }
  .cone.c1 { left: 22%; }
  .cone.c2 { right: 28%; }

  /* dust puff under crane/wall */
  .dust {
    position: absolute;
    bottom: 42px;
    width: 20px; height: 8px;
    background: radial-gradient(ellipse at center, rgba(217,211,200,0.9), transparent 70%);
    border-radius: 50%;
    animation: dustPuff 6s ease-in-out infinite;
    opacity: 0;
  }
  .dust.d1 { right: 14%; animation-delay: 1.5s; }
  .dust.d2 { left: 14%; animation-delay: 4s; }
  @keyframes dustPuff {
    0% { opacity: 0; transform: translateY(0) scale(0.5); }
    40% { opacity: 0.9; }
    100% { opacity: 0; transform: translateY(-12px) scale(1.6); }
  }

  .foot {
    position: relative;
    z-index: 3;
    font-family: "JetBrains Mono", ui-monospace, monospace;
    font-size: 11px;
    color: var(--ink-2);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    display: flex;
    justify-content: space-between;
    padding: 12px 36px 14px;
  }

  /* progress bar */
  .progressWrap {
    margin-top: 12px;
    width: min(420px, 80%);
  }
  .progress {
    height: 6px;
    background: rgba(23,32,43,0.08);
    border-radius: 999px;
    overflow: hidden;
    position: relative;
  }
  .progress > span {
    position: absolute;
    inset: 0;
    width: 38%;
    background: linear-gradient(to right, var(--blue), var(--blue-deep));
    border-radius: 999px;
    animation: progressPulse 4.2s ease-in-out infinite;
  }
  @keyframes progressPulse {
    0%,100% { width: 34%; }
    50% { width: 62%; }
  }
  .progressLabel {
    margin-top: 8px;
    display: flex; justify-content: space-between;
    font-family: "JetBrains Mono", ui-monospace, monospace;
    font-size: 10px;
    letter-spacing: 0.18em;
    color: var(--ink-2);
    text-transform: uppercase;
  }

  @media (max-width: 640px) {
    .topbar { padding: 16px 18px; }
    .meta .hideS { display: none; }
    .foot { padding: 10px 18px 12px; flex-direction: column; gap: 4px; align-items: flex-start; }
    .scaffold, .crane { transform: scale(0.75); transform-origin: bottom center; }
  }

